# Sales Rep Copilot — RAG Project Roadmap
*Last updated: May 2026*

---

## Project Goal

A Streamlit web app where a sales rep types a natural language question about a B2B customer and gets a grounded, data-driven answer generated by Claude API — powered by a RAG pipeline over real transactional data.

**Example questions the system answers:**
- "Summarize customer ABC Corp"
- "Which Enterprise customers haven't ordered in 60 days?"
- "What is the RFM persona and contract status of customer X?"

---

## Purpose

Portfolio project to close 3 critical skill gaps simultaneously:
- RAG / embeddings
- MLflow experiment tracking
- Docker containerization

---

## Data Sources

| File | Key Fields |
|---|---|
| Customers.csv | cus_code, cus_name, Segment, RFMPersonas, NumberOfEmployees, SalesPerson |
| Transactions.csv | cus_code, trx_date, trx_amt, vou_label, MCC |
| Orders.csv | CustomerCode, Amount, TotalRevenue, Product, OrderType, PaymentType |
| Contracts.csv | cus_code, cuv_contract_sign_date, cuv_closed_date, Potential, Payment |

---

## Full Stack

| Phase | Tools |
|---|---|
| Data prep | pandas |
| Embeddings | sentence-transformers (local, free) |
| Vector store | ChromaDB |
| Orchestration | LangChain |
| LLM | Claude API |
| UI | Streamlit |
| Experiment tracking | MLflow |
| Containerization | Docker |

---

## Architecture

```
Sales rep → Streamlit UI (types question)
                ↓
          LangChain pipeline
                ↓
          ChromaDB retrieval (semantic search over customer profiles)
                ↓
          Claude API generates grounded answer
                ↓
          Answer displayed in Streamlit UI
```

---

## Phase 1 — Data Preparation
**Timeline:** Weekend 1, Day 1 morning

**Goal:** Load all CSVs, merge on cus_code, aggregate everything per customer into one clean row.

**Why:** RAG cannot work on raw database rows. One customer may have 500 transaction rows. You need one coherent summary per customer as the foundation for everything downstream.

**Output:** A pandas DataFrame — one row per customer, all info aggregated.

**Key decisions:**
- Aggregate transactions: total amount, avg amount, last transaction date, count
- Aggregate orders: total revenue, avg order value, last order date, count, most common product
- Contracts: status (active/closed), sign date, closed date, payment type
- Customers: segment, RFM persona, number of employees, salesperson

---

## Phase 2 — Text Profile Generation
**Timeline:** Weekend 1, Day 1 afternoon

**Goal:** Convert each aggregated customer row into a human-readable text paragraph.

**Why:** This text is what gets embedded and stored. The quality of this text directly determines the quality of answers. Garbage in, garbage out.

**Example output:**
```
"Customer: ABC Corp | Segment: Enterprise | RFM: Champion |
Employees: 450 | Total Orders: 23 | Avg Order Value: €4,200 |
Last Order: March 2025 | Contract Status: Active |
Signed: Jan 2024 | Salesperson: Nikos P."
```

**Output:** A list of text strings, one per customer.

---

## Phase 3 — Embedding + Vector Store
**Timeline:** Weekend 1, Day 2

**Goal:** Convert each text profile into a vector and store in ChromaDB.

**Why:** This is the Retrieval in RAG. When a user asks a question, the system finds semantically similar profiles — not keyword matches. This is the core technical skill every GenAI JD is asking for.

**Tools:**
- `sentence-transformers` — creates embeddings locally, free, no API cost
- `ChromaDB` — local vector database, zero infrastructure needed

**Output:** A persistent ChromaDB collection with all customer profiles embedded and indexed.

---

## Phase 4 — RAG Pipeline
**Timeline:** Weekend 2, Day 1

**Goal:** Build the question → retrieve → answer loop.

**Step by step:**
1. User types a question in Streamlit
2. Question gets embedded using the same sentence-transformers model
3. ChromaDB finds top-N most semantically similar customer profiles
4. Retrieved profiles passed as context to Claude API
5. Claude generates a grounded answer
6. Answer displayed in Streamlit UI

**Why:** This is the Augmented Generation in RAG. Claude doesn't hallucinate because it receives real retrieved data as context. This is the core architectural pattern every GenAI role requires.

**Output:** A working Streamlit app where you can ask questions and get answers.

---

## Phase 5 — MLflow + Docker
**Timeline:** Weekend 2, Day 2

**Goal:** Add experiment tracking and containerize the app.

**MLflow — what to track:**
- Embedding model used
- Number of chunks retrieved (top-N)
- Question and answer pairs
- Retrieval latency

**Docker — what to containerize:**
- The full Streamlit app
- ChromaDB collection
- All dependencies in requirements.txt

**Why it matters:** Separates a notebook experiment from a production system. Closes two CV gaps in one afternoon. Answers the interview question "how would you deploy this?"

**Output:** A Dockerized app + MLflow experiment log — both screenshottable for portfolio.

---

## CV Bullet (Final)

```
§ Built a RAG-based Sales Copilot over B2B transactional data
  (customers, contracts, orders, transactions) using LangChain,
  ChromaDB, and Claude API — enabling natural language querying
  of customer behavior and commercial signals. Deployed as an
  interactive Streamlit application, tracked experiments with
  MLflow, and containerized with Docker.
```

---

## Portfolio Checklist

- [ ] GitHub repo with clean README
- [ ] Architecture diagram in README
- [ ] Short demo video (2-3 min screen recording)
- [ ] MLflow screenshot showing experiment runs
- [ ] Docker instructions in README
- [ ] CV bullet added under Edenred experience

---

## Decisions Made

- **API:** Claude API (consistent with existing production story)
- **Embeddings:** Local sentence-transformers (free, no API cost)
- **Vector DB:** ChromaDB (local, zero infra)
- **UI:** Streamlit (Python only, fast to build, portfolio-ready)
- **Scope:** Portfolio project — demo + GitHub, not live deployment
- **Data:** Real Edenred data (usage approved)

---

## Folder Structure (Locked — 2026-05-27)

```
RAG Project/
├── data/raw/           ← CSVs go here (gitignored — contains real customer data)
├── data/processed/     ← Phase 1 aggregated output
├── src/
│   ├── data_prep.py        ← Phase 1
│   ├── profile_builder.py  ← Phase 2
│   ├── embeddings.py       ← Phase 3
│   ├── rag_pipeline.py     ← Phase 4
│   └── app.py              ← Phase 4 (Streamlit)
├── chroma_db/          ← gitignored, local-persistent
├── mlflow_runs/        ← gitignored, local-persistent
└── docker/             ← Phase 5
```

**Rules:**
- Never commit anything under data/raw/ — real customer data lives there
- Never commit chroma_db/ or mlflow_runs/ — generated artifacts
- One src/ file per phase — do not split phase logic across multiple files unless explicitly decided

